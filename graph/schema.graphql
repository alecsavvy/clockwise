type User {
  id: ID!
  handle: String!
  bio: String
}

type Track {
  id: ID!
  title: String!
  streamUrl: String
  description: String
  user: User!
}

type Query {
  users: [User!]!
  tracks: [Track!]!
}
