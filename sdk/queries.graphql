mutation CreateUser(
  $handle: String!
  $address: String!
  $bio: String!
) {
  createUser(
    input: {
      handle: $handle,
      address: $address,
      bio: $bio
    }
  ) {
    handle
    address
    bio
    txhash
  }
}

